extends layout
include mixins

block content
  h1= title
  p Welcome to #{title}

    form(method='post', action='/login')
      each field in form
        +field(field, submitted)
      div: button() Innskrá
    p: a(href='/signup') Búa til notanda